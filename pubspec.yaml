name: state_management
description: Flutter Festival Songdo Example Project

publish_to: 'none'

version: 1.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.0.1
  provider: ^6.0.2
  get: ^4.6.1
  flutter_hooks: ^0.18.4
  hooks_riverpod: ^1.0.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
